

/*
	Given a filename and depth of a search, load the library given by the name.

	Right now I am having the object register itself in it's own init with a function handle to use to send messages to the object.

*/

int
LoadObject (char *name, int depth);
